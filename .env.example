# jinn-node Operator Configuration
# Copy to .env in this directory and fill in values.
# This file is loaded by the worker at startup.

# =============================================================================
# REQUIRED: Core Runtime
# =============================================================================
RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
CHAIN_ID=8453
OPERATE_PASSWORD=change_me_min_8_chars
# Optional fallback when .operate profile is unavailable.
# Used to sign Control API + credential bridge requests with ERC-8128.
# JINN_SERVICE_PRIVATE_KEY=0x...

# =============================================================================
# REQUIRED: Jinn Services
# =============================================================================
PONDER_GRAPHQL_URL=https://ponder-production-6d16.up.railway.app/graphql
CONTROL_API_URL=https://control-api-production-c1f5.up.railway.app/graphql
X402_GATEWAY_URL=https://x402-gateway-production-1b84.up.railway.app
# Control API calls are authenticated via ERC-8128 signed HTTP requests.

# =============================================================================
# REQUIRED: LLM Auth (one of these must be true)
# =============================================================================
# Option A: Gemini CLI login (recommended) — no env var needed.
# Option B: Provide creds via env for rotation.
# GEMINI_API_KEY=your_gemini_api_key
# GEMINI_OAUTH_CREDENTIALS=[{"oauth_creds":{...},"google_accounts":{"active":"you@example.com"}}]

# =============================================================================
# DEFAULTS: Staking + Workstream + Multi-operator
# =============================================================================
STAKING_CONTRACT=0x0dfaFbf570e9E813507aAE18aA08dFbA0aBc5139
WORKSTREAM_FILTER=0x9470f6f2bec6940c93fedebc0ea74bccaf270916f4693e96e8ccc586f26a89ac

# Worker picks up requests for ALL mechs staked in the Jinn staking contract.
# This enables multi-operator support: any staked mech can deliver after the
# 61-second priority window expires.
WORKER_MECH_FILTER_MODE=staking
WORKER_STAKING_CONTRACT=0x0dfaFbf570e9E813507aAE18aA08dFbA0aBc5139

# =============================================================================
# REQUIRED: Credential Bridge
# =============================================================================
# URL of the x402-gateway credential bridge (venture credentials like GitHub,
# Telegram, OpenAI etc. are fetched dynamically — not stored in this file).
CREDENTIAL_BRIDGE_URL=https://x402-gateway-production-1b84.up.railway.app

# =============================================================================
# HIGHLY RECOMMENDED (code tasks)
# =============================================================================
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=you@example.com

# =============================================================================
# OPTIONAL: Earning Schedule
# =============================================================================
# When set, worker only claims jobs during this time window (local timezone).
# Overnight windows work: "22:00-08:00" means 10pm to 8am.
# Unset = always earning.
# EARNING_SCHEDULE=22:00-08:00

# Max jobs per earning window. Unset = unlimited.
# EARNING_MAX_JOBS=10
